POSTGRES_URL=postgres://localhost:5432/yugmach_payload
DATABASE_URL=postgres://localhost:5432/yugmach_payload
PAYLOAD_SECRET=change-me-to-a-long-random-string
REVALIDATE_SECRET=change-me
# Public site URL for SEO/canonicals (can be www.yugmach.com once DNS is live)
NEXT_PUBLIC_SITE_URL=https://www.yugmach.com
# Payload admin API / Blob client uploads — MUST match the host you open /admin on
PAYLOAD_SERVER_URL=https://frontend-six-kappa-clmd7dlhna.vercel.app
NEXT_PUBLIC_WHATSAPP_NUMBER=917500399754
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=dab2jnv1e

# Vercel Blob — required for Media uploads on Vercel (Storage → Blob → Create)
BLOB_READ_WRITE_TOKEN=

# Optional — lead rate limit + email
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
RESEND_API_KEY=
LEAD_NOTIFY_EMAIL=sales@yugmach.com
LEAD_FROM_EMAIL=YugMach Leads <onboarding@resend.dev>

# One-time migration from Django Postgres (scripts/migrate-from-django.mjs)
DJANGO_DATABASE_URL=postgres://localhost:5432/yugmach_dev
